[
  {
    "id": "agents-as-principals",
    "title": "Agents as first-class principals",
    "summary": "Register an agent, get its automatic keypairs, and exchange its API key for a short-lived JWT.",
    "blocks": [
      {
        "type": "prose",
        "text": "In 1Claw an agent is a real principal with its own identity, not a shared human login. An agent can authenticate three ways: an API key with the ocv_ prefix, mutual TLS, or OIDC federation. This lesson uses the API key path because it's the fastest to run end to end."
      },
      {
        "type": "points",
        "items": [
          "API key: ocv_ random value, argon2-hashed, shown once at creation",
          "mTLS: client certificate identity for infrastructure agents",
          "OIDC: federate an external identity provider into 1Claw"
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "When you register an agent, 1Claw auto-generates an Ed25519 signing keypair and a P-256 ECDH keypair. Private keys live HSM-encrypted in the __agent-keys vault; only the public keys sit on the agent record."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Use the same 1ck_ key from Foundations. Agent keys cannot create other agents.",
            "code": "export ONECLAW_API_KEY=\"1ck_your_key_here\"",
            "lang": "bash"
          },
          {
            "text": "Register an agent. The plaintext ocv_ key is returned once in this response and never again. Use the vault UUID from Foundations (`1claw vault list`) and export it: `export VAULT_ID=...`",
            "code": "export VAULT_ID=\"your-vault-uuid-here\"\n\nAGENT_RESPONSE=$(curl -s -X POST https://api.1claw.co/v1/agents \\\n  -H \"Authorization: Bearer $ONECLAW_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"web-backend\",\"token_ttl_seconds\":900,\"vault_ids\":[\"'$VAULT_ID'\"]}')\n\necho \"$AGENT_RESPONSE\"",
            "lang": "bash"
          },
          {
            "text": "Extract the agent id and api_key from the response. The response looks like: {\"agent\":{\"id\":\"7e3ce911-...\",\"name\":\"web-backend\",...},\"api_key\":\"ocv_...\"}. Save both values as env vars.",
            "code": "export ONECLAW_AGENT_ID=$(echo \"$AGENT_RESPONSE\" | jq -r .agent.id)\nexport ONECLAW_AGENT_KEY=$(echo \"$AGENT_RESPONSE\" | jq -r .api_key)\necho \"Agent ID: $ONECLAW_AGENT_ID\"\necho \"Agent Key: $ONECLAW_AGENT_KEY\"",
            "lang": "bash"
          },
          {
            "text": "Exchange the agent credentials for a short-lived JWT. This endpoint needs no bearer token; the credentials in the body are the auth.",
            "code": "TOKEN=$(curl -s -X POST https://api.1claw.co/v1/auth/agent-token \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"agent_id\":\"'$ONECLAW_AGENT_ID'\",\"api_key\":\"'$ONECLAW_AGENT_KEY'\"}' | jq -r .access_token)",
            "lang": "bash"
          },
          {
            "text": "Confirm the token works by fetching the agent's own profile. You should see the ssh_public_key and ecdh_public_key fields.",
            "code": "curl -s https://api.1claw.co/v1/agents/me -H \"Authorization: Bearer $TOKEN\"",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "prose",
        "text": "You now have a registered agent and a fresh JWT bound to its vaults and TTL. Later calls use a Bearer JWT like this one; you will refresh it when policies change."
      }
    ],
    "quiz": [
      {
        "question": "How many times is the plaintext ocv_ API key returned?",
        "options": [
          "On every token exchange",
          "Once, at agent creation or key rotation",
          "Never, it stays in the HSM",
          "Once per day"
        ],
        "correctIndex": 1,
        "explanation": "Only the argon2 hash is stored; the plaintext is shown once at creation or rotation."
      },
      {
        "question": "Which endpoint turns an agent_id plus api_key into a JWT?",
        "options": [
          "POST /v1/agents",
          "POST /v1/auth/agent-token",
          "GET /v1/agents/me",
          "POST /v1/vaults"
        ],
        "correctIndex": 1,
        "explanation": "POST /v1/auth/agent-token takes the credentials in the body and returns a short-lived access_token."
      },
      {
        "question": "What two keypairs does 1Claw auto-generate for a new agent?",
        "options": [
          "RSA and DSA",
          "Ed25519 signing and P-256 ECDH",
          "secp256k1 and Ed25519",
          "Two AES keys"
        ],
        "correctIndex": 1,
        "explanation": "Every agent gets an Ed25519 signing keypair and a P-256 ECDH keypair at creation."
      }
    ]
  },
  {
    "id": "policies-and-scoped-access",
    "title": "Policies and scoped access",
    "summary": "Grant an agent read access to one secret path, then use deny rules and priority to carve exceptions out of a broad grant.",
    "blocks": [
      {
        "type": "prose",
        "text": "A fresh agent has zero access. Its JWT carries an empty scope list until a human writes a policy. Policies tie a principal to path patterns and permissions, so you decide exactly what an agent can touch."
      },
      {
        "type": "points",
        "items": [
          "* matches one path segment",
          "** matches zero or more segments (any depth)",
          "read: GET a value and list metadata",
          "write: PUT and DELETE at matching paths"
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "When you create, update, or delete an agent's policy, all of that agent's active JWTs are revoked immediately. The agent must re-exchange its API key to pick up the new scope."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Export your keys and IDs. Use the values saved from Lesson 1.",
            "code": "export ONECLAW_API_KEY=\"1ck_your_key_here\"\nexport VAULT_ID=\"your-vault-uuid-here\"\nexport AGENT_ID=\"your-agent-uuid-here\"",
            "lang": "bash"
          },
          {
            "text": "Create a policy that lets the agent read only api-keys/openai. The pattern is a single path, so the agent cannot reach anything else in the vault.",
            "code": "curl -s -X POST https://api.1claw.co/v1/vaults/$VAULT_ID/policies \\\n  -H \"Authorization: Bearer $ONECLAW_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"principal_type\":\"agent\",\"principal_id\":\"'$AGENT_ID'\",\"permissions\":[\"read\"],\"secret_path_pattern\":\"api-keys/openai\"}'",
            "lang": "bash"
          },
          {
            "text": "Re-exchange the agent key for a fresh JWT. The old token was revoked by the policy change.",
            "code": "TOKEN=$(curl -s -X POST https://api.1claw.co/v1/auth/agent-token \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"agent_id\":\"'$ONECLAW_AGENT_ID'\",\"api_key\":\"'$ONECLAW_AGENT_KEY'\"}' | jq -r .access_token)",
            "lang": "bash"
          },
          {
            "text": "Read the allowed path (works), then try a different path. Both 403 (forbidden) and 404 (not found) confirm the agent cannot access that path.",
            "code": "curl -s https://api.1claw.co/v1/vaults/$VAULT_ID/secrets/api-keys/openai \\\n  -H \"Authorization: Bearer $TOKEN\"\ncurl -s https://api.1claw.co/v1/vaults/$VAULT_ID/secrets/api-keys/stripe \\\n  -H \"Authorization: Bearer $TOKEN\"",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Policies also support runtime conditions: an IP allowlist (specific IPs or CIDR ranges) and a time window (allowed hours and days). Add these when an agent runs from known infrastructure."
      },
      {
        "type": "prose",
        "text": "Your agent can now read one secret path and nothing else, and any policy change takes effect on the very next request."
      },
      {
        "type": "prose",
        "text": "Policy Engine v2 (v0.47) added three fields that turn policies from a flat allow-list into a real evaluation model: effect, priority, and attribute_conditions."
      },
      {
        "type": "points",
        "items": [
          "effect — \"allow\" or \"deny\". A deny rule lets you punch a hole in a broad allow.",
          "priority — an integer; higher wins when two policies both match the same path.",
          "attribute_conditions — JSONB for fine-grained matching beyond path, IP, and time."
        ]
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Grant the agent broad read access across the vault.",
            "code": "curl -s -X POST https://api.1claw.co/v1/vaults/$VAULT_ID/policies \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"principal_type\": \"agent\", \"principal_id\": \"'$AGENT_ID'\",\n    \"path_pattern\": \"app/**\", \"permissions\": [\"read\"],\n    \"effect\": \"allow\", \"priority\": 10\n  }'",
            "lang": "bash"
          },
          {
            "text": "Now carve out the one prefix it must never read, with a higher priority so it wins the overlap.",
            "code": "curl -s -X POST https://api.1claw.co/v1/vaults/$VAULT_ID/policies \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"principal_type\": \"agent\", \"principal_id\": \"'$AGENT_ID'\",\n    \"path_pattern\": \"app/billing/**\", \"permissions\": [\"read\"],\n    \"effect\": \"deny\", \"priority\": 100\n  }'",
            "lang": "bash"
          },
          {
            "text": "Verify. The first read succeeds, the second is denied by the higher-priority deny rule.",
            "code": "1claw secret get app/search/api-key   # allowed\n1claw secret get app/billing/stripe   # denied",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Priority decides overlaps, not effect. A deny rule does not automatically beat an allow — give it the higher priority, or the broad allow may win."
      }
    ],
    "quiz": [
      {
        "question": "What access does an agent have before any policy is written?",
        "options": [
          "Full read on all vaults",
          "Read on its own vault only",
          "Zero access, empty scopes",
          "Read plus write on the default vault"
        ],
        "correctIndex": 2,
        "explanation": "1Claw is safe-by-default: no scopes and no policies means the JWT grants nothing."
      },
      {
        "question": "In a policy pattern, what does ** match?",
        "options": [
          "Exactly one segment",
          "Zero or more segments at any depth",
          "Only the vault root",
          "Nothing, it is a literal"
        ],
        "correctIndex": 1,
        "explanation": "* matches one segment while ** matches zero or more segments (any depth)."
      },
      {
        "question": "What happens to active JWTs when an agent's policy changes?",
        "options": [
          "Nothing until they expire",
          "They are revoked immediately",
          "They gain the new scope automatically",
          "Only write tokens are revoked"
        ],
        "correctIndex": 1,
        "explanation": "Any policy create, update, or delete revokes the agent's active JWTs so stale scopes cannot linger."
      },
      {
        "question": "An allow policy on app/** has priority 10 and a deny policy on app/billing/** has priority 5. What happens on a read of app/billing/stripe?",
        "options": [
          "The read is allowed, because the allow rule has higher priority",
          "The read is denied, because deny always beats allow",
          "The request errors as an unresolvable conflict",
          "Both policies are ignored"
        ],
        "correctIndex": 0,
        "explanation": "Priority resolves the overlap, and 10 beats 5. Deny does not win automatically — the deny rule needs the higher priority number to take effect."
      }
    ]
  },
  {
    "id": "cedar-and-opa-policies",
    "title": "Cedar and OPA policy backends",
    "summary": "Express authorization as declarative Cedar or Rego policy, and dry-run it before it ever denies a real request.",
    "blocks": [
      {
        "type": "prose",
        "text": "Path patterns and conditions cover most access control. When rules get genuinely complex — cross-resource logic, org-specific data lookups — 1Claw supports two declarative policy backends alongside the built-in engine."
      },
      {
        "type": "points",
        "items": [
          "Cedar (Team tier and above) — AWS's authorization language. Principal, action, resource, context.",
          "OPA (Business tier and above) — Open Policy Agent, using Rego and custom data documents.",
          "Both are additive. The built-in policy engine still runs; these express rules it cannot."
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Both backends ship a test endpoint that evaluates a policy against a hypothetical request without saving it. Use it before you create anything."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Write a Cedar policy. This one forbids any agent from reading production secrets outside business hours.",
            "code": "curl -s -X POST https://api.1claw.co/v1/org/cedar-policies \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"name\": \"no-prod-reads-after-hours\",\n    \"policy\": \"forbid(principal in Group::\\\"agents\\\", action == Action::\\\"read\\\", resource in Vault::\\\"production\\\") when { context.hour > 18 };\"\n  }'",
            "lang": "bash"
          },
          {
            "text": "Dry-run it before trusting it. Supply a principal, action, resource, and context and see the decision.",
            "code": "curl -s -X POST https://api.1claw.co/v1/org/cedar-policies/test \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"principal\": \"Agent::'$AGENT_ID'\",\n    \"action\": \"read\",\n    \"resource\": \"Vault::production\",\n    \"context\": { \"hour\": 22 }\n  }'",
            "lang": "bash"
          },
          {
            "text": "List what is active, from the CLI.",
            "code": "1claw cedar-policy list\n1claw opa-policy list",
            "lang": "bash"
          },
          {
            "text": "The OPA equivalent takes Rego plus an input document.",
            "code": "curl -s -X POST https://api.1claw.co/v1/org/opa-policies/test \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"input\": { \"agent\": { \"environment\": \"preview\" }, \"path\": \"prod/db\" } }'",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Cedar is Team+ and OPA is Business+. On a lower tier these endpoints exist but return a tier error, so check your plan before designing around them."
      }
    ],
    "quiz": [
      {
        "question": "What is the purpose of the /v1/org/cedar-policies/test endpoint?",
        "options": [
          "Dry-run a policy against a hypothetical request without saving it",
          "Verify the policy's syntax only",
          "Load-test the policy engine",
          "Apply the policy to every past request"
        ],
        "correctIndex": 0,
        "explanation": "The test endpoint evaluates a full principal/action/resource/context request and returns the decision, so you can confirm behaviour before a policy affects live traffic."
      },
      {
        "question": "How do Cedar and OPA policies relate to the built-in policy engine?",
        "options": [
          "They are additive — the built-in engine still runs",
          "They replace the built-in engine entirely",
          "They only apply to human users",
          "They only apply to on-chain signing"
        ],
        "correctIndex": 0,
        "explanation": "Both are additional backends layered alongside the built-in engine, for rules that path patterns and conditions cannot express."
      },
      {
        "question": "Which tiers gate Cedar and OPA respectively?",
        "options": [
          "Cedar is Team+, OPA is Business+",
          "Both are Free",
          "Cedar is Business+, OPA is Team+",
          "Both are Enterprise-only"
        ],
        "correctIndex": 0,
        "explanation": "Cedar requires Team or above; OPA requires Business or above. On lower tiers the endpoints return a tier error."
      }
    ]
  },
  {
    "id": "agent-self-enrollment-and-approvals",
    "title": "Agent self-enrollment and approvals",
    "summary": "Let an agent enroll itself with no credentials and route the approval to a human.",
    "blocks": [
      {
        "type": "prose",
        "text": "This creates a new agent via self-enrollment — a different bootstrap path from Lesson 1's human-initiated registration. Sometimes the agent boots before anyone has provisioned it. The enroll endpoint is public, so an agent can request its own identity and wait for a human to approve. The API key is emailed to the human after approval and never returned to the agent."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "Pick one of the two enrollment methods below."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Option A: Submit an enrollment with a name and a human_email. Allow and Deny links are sent to that address. No credentials are required.",
            "code": "curl -s -X POST https://api.1claw.co/v1/agents/enroll \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"deploy-bot\",\"human_email\":\"alice@example.com\",\"description\":\"CI pipeline agent\"}'",
            "lang": "bash"
          },
          {
            "text": "Option B: Send name only. The response includes an enrollment_id and an approval_url the human opens while signed in. No email needed. Sample response: {\"enrollment_id\":\"enr_abc123\",\"approval_url\":\"https://1claw.co/approve/enr_abc123\",\"status\":\"pending\"}",
            "code": "curl -s -X POST https://api.1claw.co/v1/agents/enroll \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"deploy-bot\"}'",
            "lang": "bash"
          },
          {
            "text": "The human approves via the emailed link or the approval_url. After approval an agent is created in their org and the ocv_ key is emailed to them.",
            "lang": "text"
          },
          {
            "text": "The human then writes a policy so the new agent can read the paths it needs. Follow Lesson 2 (Policies and scoped access) in this track.",
            "lang": "text"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "For sensitive actions an agent can also file an approval with POST /v1/approvals/request, directed to its creator. Requests carry a risk tier: T1 is informational, T2 requires biometric step-up, and T3 requires passkey plus TOTP attestation before the human can approve."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Enrollment is rate limited: one per email per 10 minutes when email is used, plus caps on pending rows and per-IP limits. Some branches return a uniform 201 to limit email enumeration."
      },
      {
        "type": "prose",
        "text": "You can now bootstrap an agent from zero credentials and hand the approval to a human, who receives the key and grants scoped access."
      }
    ],
    "quiz": [
      {
        "question": "Where does the enrolled agent's API key go?",
        "options": [
          "Returned in the enroll response",
          "Emailed to the human after approval",
          "Stored in the agent's env",
          "Printed to the server log"
        ],
        "correctIndex": 1,
        "explanation": "The plaintext key is emailed to the approving human and is never returned from the enroll endpoint."
      },
      {
        "question": "Which risk tier requires passkey plus TOTP attestation?",
        "options": [
          "T1",
          "T2",
          "T3",
          "None require step-up"
        ],
        "correctIndex": 2,
        "explanation": "T1 is informational, T2 requires biometric, and T3 requires passkey plus TOTP."
      },
      {
        "question": "What can an agent submit with name only and no email?",
        "options": [
          "Nothing, email is mandatory",
          "A link-only enrollment returning an approval_url",
          "A full agent with an immediate key",
          "A policy change"
        ],
        "correctIndex": 1,
        "explanation": "Name-only enrollment creates a link-only pending enrollment and returns an approval_url for a signed-in human."
      }
    ]
  },
  {
    "id": "mcp-server-integration",
    "title": "MCP server integration",
    "summary": "Connect an AI client to the 1Claw MCP server and run a first list_secrets call.",
    "blocks": [
      {
        "type": "prose",
        "text": "The 1Claw MCP server gives an AI client just-in-time access to vault secrets over the Model Context Protocol. It exposes 141 tools (vault, Intents signing, Execution Intents bindings, treasury, approvals, automations, runtimes, memory, chat, channels, Cedar/OPA policy backends, and Safe agent accounts). The canonical setup is local stdio — `npx @1claw/mcp` with a single ONECLAW_AGENT_API_KEY env var; the server exchanges it for a JWT, refreshes before expiry, and auto-discovers the agent and vault. The MCP server itself enforces nothing: every call is checked against the agent's policies by the vault API."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Requires: 1Claw account, a vault with secrets, an agent, and a read policy (see Lessons 1–2)."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Recommended: local stdio. Run the server as a process and pass only the agent API key. It exchanges the key for a JWT, auto-refreshes it, and auto-discovers the agent and vault — no agent_id, no vault ID, no manual token minting. Paste this into .cursor/mcp.json, claude_desktop_config.json, or any MCP client that supports stdio.",
            "code": "{\n  \"mcpServers\": {\n    \"1claw\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@1claw/mcp\"],\n      \"env\": { \"ONECLAW_AGENT_API_KEY\": \"ocv_your_agent_api_key\" }\n    }\n  }\n}",
            "lang": "json"
          },
          {
            "text": "Mint an agent JWT. Use env vars from Lesson 1 or paste your agent_id and ocv_ key directly.",
            "code": "export ONECLAW_AGENT_ID=\"your-agent-uuid\"\nexport ONECLAW_AGENT_KEY=\"ocv_your_agent_api_key\"\nexport VAULT_ID=\"your-vault-uuid\"\n\nTOKEN=$(curl -s -X POST https://api.1claw.co/v1/auth/agent-token \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"agent_id\":\"'$ONECLAW_AGENT_ID'\",\"api_key\":\"'$ONECLAW_AGENT_KEY'\"}' | jq -r .access_token)",
            "lang": "bash"
          },
          {
            "text": "Alternative: hosted server. Only for non-IDE callers that can mint a fresh JWT per connection (the token from the previous step). Paste your $TOKEN and $VAULT_ID into the headers.",
            "code": "{\n  \"mcpServers\": {\n    \"1claw\": {\n      \"url\": \"https://mcp.1claw.co/mcp\",\n      \"headers\": {\n        \"Authorization\": \"Bearer <paste your $TOKEN here>\",\n        \"X-Vault-ID\": \"<paste your $VAULT_ID here>\"\n      }\n    }\n  }\n}",
            "lang": "json"
          },
          {
            "text": "Restart the client and ask it to list your secrets. The agent calls list_secrets and returns paths and metadata, never values.",
            "code": "List the secrets in my 1claw vault.",
            "lang": "text"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Do not point an IDE at the hosted https://mcp.1claw.co/mcp URL with a static Bearer token. Agent JWTs expire in about 15 minutes by default, so the connection dies mid-session and the client will not re-mint it. Use the stdio server for Cursor, Claude Desktop, VS Code, and Claude Code — it refreshes the token for you."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Output inspection runs by default. If a known secret value ever appears in a non-secret tool's output, it is replaced with [REDACTED:#hashprefix] before it reaches the model context."
      },
      {
        "type": "prose",
        "text": "Your AI client is now wired to the vault, and a first list_secrets call returns metadata scoped to the agent's policies."
      }
    ],
    "quiz": [
      {
        "question": "Which two headers authenticate a hosted MCP connection?",
        "options": [
          "Authorization and X-Vault-ID",
          "X-Api-Key and X-Agent-ID",
          "Cookie and X-Vault-ID",
          "Authorization and X-Client-Share"
        ],
        "correctIndex": 0,
        "explanation": "The hosted server takes a Bearer JWT in Authorization and the vault UUID in X-Vault-ID."
      },
      {
        "question": "Who enforces access control for MCP tool calls?",
        "options": [
          "The MCP server itself",
          "The vault API policy engine",
          "The AI client",
          "The LLM provider"
        ],
        "correctIndex": 1,
        "explanation": "The MCP server just relays requests; the vault API checks every call against the agent's policies."
      },
      {
        "question": "What does list_secrets return?",
        "options": [
          "Decrypted values",
          "Paths, types, versions, and metadata only",
          "Only the vault name",
          "The agent's private keys"
        ],
        "correctIndex": 1,
        "explanation": "list_secrets returns metadata and never secret values; use get_secret for a value."
      }
    ]
  },
  {
    "id": "execution-intents-bindings",
    "title": "Execution Intents and bindings",
    "summary": "Let an agent call an external API through a binding, so the credential is injected server-side and never enters the model's context.",
    "blocks": [
      {
        "type": "prose",
        "text": "Policies control which secrets an agent can read. Execution Intents go further: the agent never reads the credential at all. You pre-configure a binding that holds the credential server-side, and the agent asks 1Claw to perform the call on its behalf."
      },
      {
        "type": "points",
        "items": [
          "The agent sends an intent — method, path, body — not a key.",
          "1Claw injects the credential, applies host and path allowlists, and makes the request.",
          "The response comes back to the agent. The secret was never in its context window.",
          "Revoking a binding cuts access instantly, with no secret to rotate."
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "This is the pattern 1claw.co leads with on its own homepage: credentials stay server-side and agents execute through bindings."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Create an HTTP binding holding a Stripe key. The credential is stored server-side against the binding.",
            "code": "curl -s -X POST https://api.1claw.co/v1/agents/$AGENT_ID/bindings \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"name\": \"stripe-api\",\n    \"binding_type\": \"http\",\n    \"base_url\": \"https://api.stripe.com\",\n    \"allowed_hosts\": [\"api.stripe.com\"],\n    \"allowed_paths\": [\"/v1/balance\", \"/v1/charges\"],\n    \"credential\": { \"type\": \"bearer\", \"value\": \"sk_live_...\" }\n  }'",
            "lang": "bash"
          },
          {
            "text": "Prefer a live pointer over a copied credential. With credential_source vault_ref the value is resolved from the vault at execution time, so rotating the secret needs no binding update.",
            "code": "curl -s -X POST https://api.1claw.co/v1/agents/$AGENT_ID/bindings \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"name\": \"stripe-api-live\",\n    \"binding_type\": \"http\",\n    \"base_url\": \"https://api.stripe.com\",\n    \"allowed_hosts\": [\"api.stripe.com\"],\n    \"credential_source\": { \"type\": \"vault_ref\", \"vault_id\": \"'$VAULT_ID'\", \"path\": \"providers/stripe/key\" }\n  }'",
            "lang": "bash"
          },
          {
            "text": "Test the binding before an agent depends on it.",
            "code": "1claw agent binding test stripe-api",
            "lang": "bash"
          },
          {
            "text": "Execute through it. This is what the agent calls — note there is no credential anywhere in the request.",
            "code": "curl -s -X POST https://api.1claw.co/v1/agents/$AGENT_ID/execute \\\n  -H \"Authorization: Bearer $AGENT_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{ \"binding\": \"stripe-api\", \"intent_type\": \"http\", \"params\": { \"method\": \"GET\", \"path\": \"/v1/balance\" } }'",
            "lang": "bash"
          },
          {
            "text": "From the SDK, the same call reads the way the homepage example does.",
            "code": "const result = await client.bindings.execute(agentId, {\n  binding: \"stripe-api\",\n  intent_type: \"http\",\n  params: { method: \"GET\", path: \"/v1/balance\" },\n});",
            "lang": "ts"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Binding types beyond HTTP: GraphQL (Pro), plus Postgres, MySQL, Redis, gRPC, SMTP, Cloud SDK, S3 and Custom on Team and above. Business+ can run execution inside the TEE with execution_surface set to tee."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Set execution_require_tee on the agent to force every execution through Shroud AND block the agent's direct secret reads outright. It is the strongest form of this pattern, but it requires execution_intents_enabled first."
      }
    ],
    "quiz": [
      {
        "question": "What is the main security advantage of executing through a binding rather than reading the secret?",
        "options": [
          "The credential is injected server-side and never enters the agent's context",
          "The request is faster",
          "The credential is rotated after every call",
          "The agent gets a longer-lived token"
        ],
        "correctIndex": 0,
        "explanation": "The agent sends an intent, not a key. The credential stays server-side, so it cannot leak into the context window, logs, or model memory."
      },
      {
        "question": "Why choose credential_source vault_ref over an inline credential?",
        "options": [
          "The credential resolves from the vault at execution time, so rotation needs no binding update",
          "It encrypts the credential more strongly",
          "It removes the need for allowed_hosts",
          "It makes the binding usable by every agent"
        ],
        "correctIndex": 0,
        "explanation": "Inline copies the credential at creation. A vault_ref is a live pointer resolved at execution, so rotating the underlying secret is picked up automatically."
      },
      {
        "question": "What does execution_require_tee do beyond forcing execution through Shroud?",
        "options": [
          "It also blocks the agent's direct secret reads",
          "It disables the agent's signing keys",
          "It grants the agent Business tier features",
          "It exempts the agent from binding allowlists"
        ],
        "correctIndex": 0,
        "explanation": "It forces execution through the TEE and blocks direct secret reads by that agent, pushing it entirely onto bindings. It requires execution_intents_enabled to be on first."
      }
    ]
  },
  {
    "id": "agent-to-human-sharing",
    "title": "Agent-to-human sharing",
    "summary": "Let an agent hand a credential it discovered or generated back to the human who created it, without knowing their email.",
    "blocks": [
      {
        "type": "prose",
        "text": "Sharing usually runs human to human. The reverse direction matters for autonomous work: an agent provisions a resource, gets a credential, and needs to hand it to a person. It should not need to know who that person is."
      },
      {
        "type": "prose",
        "text": "Setting recipient_type to \"creator\" solves this. The API resolves the recipient from the agent's own created_by field — no email, no user ID, nothing the agent could get wrong or leak."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Requires: an agent with write access to a vault, from the earlier lessons in this track."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "recipient_type accepts user, agent, external_email, anyone_with_link, or creator. Only creator resolves the recipient from the agent's own created_by field."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "As the agent, share the secret back to its creator. Shares are addressed by secret ID, and expires_at is required.",
            "code": "curl -s -X POST https://api.1claw.co/v1/secrets/$SECRET_ID/share \\\n  -H \"Authorization: Bearer $AGENT_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"recipient_type\": \"creator\",\n    \"permissions\": [\"read\"],\n    \"expires_at\": \"2026-12-31T23:59:59Z\"\n  }'",
            "lang": "bash"
          },
          {
            "text": "As the human, list what is waiting in the Inbound tab.",
            "code": "1claw share list --inbound",
            "lang": "bash"
          },
          {
            "text": "Accept it. Until you do, the share is pending and grants nothing.",
            "code": "1claw share accept <share-id>",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "This is the clean pattern for agents that generate credentials: the agent creates the value, shares it to its creator, and the human accepts. The agent never needs directory access or an email address."
      }
    ],
    "quiz": [
      {
        "question": "Why use recipient_type \"creator\" instead of the human's email?",
        "options": [
          "The API resolves the recipient from the agent's created_by field, so the agent needs no identity data",
          "It delivers the share faster",
          "It skips the acceptance step",
          "It grants write access automatically"
        ],
        "correctIndex": 0,
        "explanation": "The agent does not need to know or store who its human is. The server resolves it from created_by, which removes both a failure mode and a small data-leak surface."
      },
      {
        "question": "What happens to a share before the human accepts it?",
        "options": [
          "It is pending and grants nothing",
          "The recipient already has full access",
          "It expires after one minute",
          "It is auto-accepted for the creator"
        ],
        "correctIndex": 0,
        "explanation": "Shares are pending until accepted. The recipient sees it in their Inbound tab and must accept before any access is granted."
      }
    ]
  },
  {
    "id": "sub-agent-delegation",
    "title": "Sub-agents and delegation",
    "summary": "Let one agent hand work to another under a human-authored delegation, with tool allowlists, depth limits, and daily caps.",
    "blocks": [
      {
        "type": "prose",
        "text": "Agents can delegate tasks to other agents. The authorization for that is never something the agents arrange between themselves — a human creates an explicit delegation record, and agents cannot create, modify, or revoke their own (403)."
      },
      {
        "type": "points",
        "items": [
          "allowed_tools / blocked_tools — exactly which tools the delegate may use.",
          "max_daily_delegations — a rate limit counted from real invocations.",
          "max_depth (1–10) — stops recursive chains of agents calling agents.",
          "delegation_mode — caller (delegate uses its own credentials, the default), target (uses the target agent's config), or both.",
          "expires_at — delegations can be temporary."
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Self-delegation is blocked with a 400, and an agent in one org can never delegate to an agent in another."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "As a human, authorize agent A to delegate to agent B, limited to two tools and ten calls a day.",
            "code": "curl -s -X POST https://api.1claw.co/v1/agents/$AGENT_A/delegations \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"delegate_id\": \"'$AGENT_B'\",\n    \"allowed_tools\": [\"search_agent_directory\", \"get_memory\"],\n    \"max_daily_delegations\": 10,\n    \"max_depth\": 2,\n    \"delegation_mode\": \"caller\"\n  }'",
            "lang": "bash"
          },
          {
            "text": "The delegating agent discovers what it is actually allowed to do at runtime.",
            "code": "curl -s https://api.1claw.co/v1/agents/$AGENT_A/delegations/effective \\\n  -H \"Authorization: Bearer $AGENT_TOKEN\"",
            "lang": "bash"
          },
          {
            "text": "Delegate a task. The X-Delegation-Depth header is tracked and enforced against max_depth.",
            "code": "1claw agent delegation list\n# via MCP the agent calls: delegate_task",
            "lang": "bash"
          },
          {
            "text": "Revoke it. Human-only, and it takes effect immediately.",
            "code": "1claw agent delegation revoke <delegation-id>",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Every delegation emits audit events: agent.delegation.created, .updated, .revoked, .invoked, and .blocked. A blocked event is the one to alert on — it means an agent tried something outside its grant."
      }
    ],
    "quiz": [
      {
        "question": "Who can create a delegation between two agents?",
        "options": [
          "A human — agents get a 403 creating their own",
          "Either agent involved",
          "The delegating agent only",
          "Any agent in the org"
        ],
        "correctIndex": 0,
        "explanation": "Delegation records are human-authored. Agents cannot create, modify, or revoke their own delegations; those calls return 403."
      },
      {
        "question": "What does max_depth protect against?",
        "options": [
          "Recursive chains of agents delegating onward indefinitely",
          "Too many tools being allowed",
          "Delegations lasting too long",
          "Cross-organization delegation"
        ],
        "correctIndex": 0,
        "explanation": "Depth is tracked with the X-Delegation-Depth header and capped at max_depth (1–10), which stops A→B→C→… chains from running away."
      },
      {
        "question": "In the default \"caller\" delegation mode, whose credentials does the delegate use?",
        "options": [
          "Its own",
          "The delegating agent's",
          "The human creator's",
          "A temporary shared credential"
        ],
        "correctIndex": 0,
        "explanation": "In caller mode the delegate acts with its own credentials and its own scopes. target mode instead runs it with the target agent's config and tools."
      }
    ]
  },
  {
    "id": "agent-discovery-a2a",
    "title": "Agent discovery and A2A",
    "summary": "Publish an agent card to the public directory so other agents can find it over A2A and MCP.",
    "blocks": [
      {
        "type": "prose",
        "text": "Agent Discovery turns a private agent into something other agents can find. An agent card advertises what it does, which protocols it speaks, and where to reach it — A2A URL, MCP endpoint, categories, and pricing."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Discovery is opt-in and human-controlled. PATCH /v1/agents/{id}/discovery is human-only; an agent cannot publish itself."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Publish the agent with a capability card.",
            "code": "curl -s -X PATCH https://api.1claw.co/v1/agents/$AGENT_ID/discovery \\\n  -H \"Authorization: Bearer $ONECLAW_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"discoverable\": true,\n    \"categories\": [\"research\", \"data\"],\n    \"description\": \"Summarizes on-chain activity for a given address.\",\n    \"protocols\": [\"a2a\", \"mcp\"]\n  }'",
            "lang": "bash"
          },
          {
            "text": "Fetch the public card. No auth needed — this is what other agents read.",
            "code": "curl -s https://api.1claw.co/v1/agents/$AGENT_ID/card",
            "lang": "bash"
          },
          {
            "text": "Search the directory, also public.",
            "code": "curl -s \"https://api.1claw.co/v1/agents/directory?q=research\"\n# or: 1claw directory search research",
            "lang": "bash"
          },
          {
            "text": "From an agent over MCP, the same search is one tool call.",
            "code": "# MCP tool: search_agent_directory\n{ \"query\": \"research\", \"categories\": [\"data\"] }",
            "lang": "json"
          }
        ]
      },
      {
        "type": "prose",
        "text": "Pair this with the previous lesson and the loop closes: an agent discovers a peer in the directory, and a human-authored delegation decides whether it may actually hand it work."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "The directory and card endpoints are unauthenticated by design. Publish only what you would put on a public web page — the card is not a place for internal detail."
      }
    ],
    "quiz": [
      {
        "question": "Can an agent publish itself to the public directory?",
        "options": [
          "No — updating discovery settings is human-only",
          "Yes, with its own token",
          "Yes, if it has the discovery scope",
          "Only if it is Business tier"
        ],
        "correctIndex": 0,
        "explanation": "PATCH /v1/agents/{id}/discovery is human-only. Discovery is opt-in and stays under human control."
      },
      {
        "question": "Which discovery endpoints require no authentication?",
        "options": [
          "The directory search and the agent card",
          "Only the directory search",
          "Only the agent card",
          "Neither — both need an agent JWT"
        ],
        "correctIndex": 0,
        "explanation": "GET /v1/agents/directory and GET /v1/agents/{id}/card are both public, which is what lets other agents discover peers before any trust exists."
      },
      {
        "question": "Discovering an agent in the directory means you can delegate work to it.",
        "options": [
          "False — delegation still needs a human-authored delegation record",
          "True, discovery implies permission",
          "True, if both agents are in the same org",
          "True, if the card lists the tool"
        ],
        "correctIndex": 0,
        "explanation": "Discovery and authorization are separate. Finding an agent tells you it exists; a human-created delegation decides whether work can actually be handed to it."
      }
    ]
  }
]
