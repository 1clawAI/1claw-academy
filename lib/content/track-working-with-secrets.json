[
  {
    "id": "cli-for-cicd",
    "title": "The CLI in CI/CD",
    "summary": "Inject vault secrets into any process with env pull, push, and run, using ONECLAW_TOKEN and ONECLAW_VAULT_ID in CI.",
    "blocks": [
      {
        "type": "prose",
        "text": "In a pipeline you don't want secrets committed to a repo or pasted into CI settings one by one. The CLI pulls them from a vault at runtime and injects them into your process."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "In CI, authenticate with env vars instead of 1claw login: set ONECLAW_TOKEN (a JWT) or ONECLAW_API_KEY, plus ONECLAW_VAULT_ID for the vault to read."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Set the non-interactive credentials in your shell or CI secrets store.",
            "code": "export ONECLAW_TOKEN=\"your-jwt\"\nexport ONECLAW_VAULT_ID=\"your-vault-uuid\"",
            "lang": "bash"
          },
          {
            "text": "Pull secrets into a .env file for tooling that reads one.",
            "code": "1claw env pull -o .env.production",
            "lang": "bash"
          },
          {
            "text": "Or skip the file entirely and run a command with secrets injected as env vars. This is the safest option since nothing lands on disk.",
            "code": "1claw env run -- npm start",
            "lang": "bash"
          },
          {
            "text": "Push a local .env up to the vault when you need to seed it.",
            "code": "1claw env push .env",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "code",
        "code": "- name: Deploy with secrets\n  env:\n    ONECLAW_TOKEN: ${{ secrets.ONECLAW_TOKEN }}\n    ONECLAW_VAULT_ID: ${{ secrets.ONECLAW_VAULT_ID }}\n  run: |\n    npx @1claw/cli env pull -o .env.production\n    npm run deploy",
        "lang": "yaml",
        "caption": "GitHub Actions step that pulls secrets from the vault"
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "For offline or flaky-network builds, run 1claw env cache to store secrets in a local AES-256-GCM encrypted file (default TTL 300s). env run uses a valid cache automatically; add --no-cache to bypass it."
      },
      {
        "type": "prose",
        "text": "Your build process now starts with secrets injected straight from the vault, with no plaintext secrets checked into the repo or pasted into CI config."
      }
    ],
    "quiz": [
      {
        "question": "Which command runs a process with vault secrets injected as env vars and nothing written to disk?",
        "options": [
          "1claw env pull",
          "1claw env run -- <cmd>",
          "1claw secret get",
          "1claw vault create"
        ],
        "correctIndex": 1,
        "explanation": "1claw env run -- <cmd> injects secrets into the process environment directly."
      },
      {
        "question": "Which two env vars typically authenticate and scope the CLI in CI?",
        "options": [
          "ONECLAW_USER and ONECLAW_PASS",
          "ONECLAW_TOKEN and ONECLAW_VAULT_ID",
          "PATH and HOME",
          "CI_TOKEN and CI_VAULT"
        ],
        "correctIndex": 1,
        "explanation": "ONECLAW_TOKEN (or ONECLAW_API_KEY) authenticates and ONECLAW_VAULT_ID scopes vault commands."
      },
      {
        "question": "What does 1claw env cache store secrets in?",
        "options": [
          "Plaintext in /tmp",
          "An AES-256-GCM encrypted local file",
          "The system keychain only",
          "A remote S3 bucket"
        ],
        "correctIndex": 1,
        "explanation": "env cache writes an AES-256-GCM encrypted local file for offline env run."
      }
    ]
  },
  {
    "id": "the-typescript-sdk",
    "title": "The TypeScript SDK",
    "summary": "Install @1claw/sdk, create a client, and fetch a secret using the {data, error} response envelope.",
    "blocks": [
      {
        "type": "prose",
        "text": "The SDK gives Node apps and agents typed, full-parity access to the API. Every call returns an envelope so you check for errors without try/catch everywhere."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "Auth options when creating a client: a personal API key (apiKey: '1ck_...') for server-side work, agent credentials (agentId + apiKey: 'ocv_...') which the SDK exchanges for a JWT and refreshes automatically, or manual login."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Install the SDK in a Node 20+ project.",
            "code": "npm install @1claw/sdk",
            "lang": "bash"
          },
          {
            "text": "Set your key in the environment so it stays out of source.",
            "code": "export ONECLAW_API_KEY=\"1ck_your_key\"",
            "lang": "bash"
          },
          {
            "text": "Write a script that creates a client and fetches a secret. Check res.error before using res.data.",
            "code": "import { createClient } from \"@1claw/sdk\";\n\nconst client = createClient({\n  baseUrl: \"https://api.1claw.xyz\",\n  apiKey: process.env.ONECLAW_API_KEY,\n});\n\nconst res = await client.secrets.get(\"YOUR_VAULT_ID\", \"api-keys/openai\");\nif (res.error) {\n  console.error(res.error.message);\n} else {\n  console.log(res.data.value); // use securely, don't log in production\n}",
            "lang": "typescript"
          },
          {
            "text": "Run it.",
            "code": "node --experimental-strip-types fetch-secret.ts",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Every method returns { data, error, meta }. Typed error classes like AuthError (401), NotFoundError (404), and RateLimitError (429) are exported from @1claw/sdk if you prefer to switch on the type."
      },
      {
        "type": "prose",
        "text": "You have a running script that authenticates with the SDK and prints a fetched secret value. That's the foundation for any Node service or agent that needs vault access."
      }
    ],
    "quiz": [
      {
        "question": "What shape does every SDK method return?",
        "options": [
          "A raw string",
          "{ data, error, meta }",
          "A Promise that throws on any non-200",
          "An array of secrets"
        ],
        "correctIndex": 1,
        "explanation": "The SDK returns a { data, error, meta } envelope on every call."
      },
      {
        "question": "How do you construct a client for server-side work with a personal key?",
        "options": [
          "createClient({ baseUrl, apiKey: '1ck_...' })",
          "new Client('1ck_...')",
          "connect('1ck_...')",
          "createClient({ password: '...' })"
        ],
        "correctIndex": 0,
        "explanation": "Pass baseUrl and apiKey (the 1ck_ personal key) to createClient."
      },
      {
        "question": "When you pass agentId + apiKey (ocv_) to createClient, what does the SDK handle for you?",
        "options": [
          "Nothing, you refresh manually",
          "Exchanging credentials for a JWT and refreshing it before expiry",
          "Creating a vault",
          "Encrypting your disk"
        ],
        "correctIndex": 1,
        "explanation": "With agent credentials the SDK exchanges them for a JWT and refreshes it automatically."
      }
    ]
  },
  {
    "id": "secret-versioning-and-rotation",
    "title": "Versioning and Rotation",
    "summary": "Rotate secrets server-side or with a client-supplied value, list version history, and disable old versions.",
    "blocks": [
      {
        "type": "prose",
        "text": "Every write to a path creates a new version and keeps the old ones. You can rotate in two ways: PUT a new value yourself, or have the vault generate a random one server-side so the plaintext never touches your client."
      },
      {
        "type": "points",
        "items": [
          "Client-supplied: PUT a new value to the same path, creating a new version",
          "Server-side: POST to secret-rotate, the vault generates and stores the value",
          "List: GET secret-versions returns all versions newest first with is_disabled",
          "Disable: a disabled version is kept for audit but reads return 410 Gone"
        ]
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Set your token and vault id.",
            "code": "export TOKEN=\"your-jwt\"\nexport VAULT_ID=\"your-vault-uuid\"",
            "lang": "bash"
          },
          {
            "text": "Rotate a secret server-side. The vault generates a random 32-char hex value and stores it as the next version. You never see or send the plaintext.",
            "code": "curl -s -X POST \"https://api.1claw.xyz/v1/vaults/$VAULT_ID/secret-rotate/api-keys%2Fdb-password\" \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"length\":32,\"charset\":\"hex\"}'",
            "lang": "bash"
          },
          {
            "text": "Or do the same from the CLI.",
            "code": "1claw secret rotate api-keys/db-password --generate --length 32 --charset hex",
            "lang": "bash"
          },
          {
            "text": "List versions to confirm a new one exists.",
            "code": "1claw secret versions api-keys/db-password",
            "lang": "bash"
          },
          {
            "text": "Disable an old version so any read of it returns 410 Gone.",
            "code": "curl -s -X POST \"https://api.1claw.xyz/v1/vaults/$VAULT_ID/secret-version/api-keys%2Fdb-password/1/disable\" \\\n  -H \"Authorization: Bearer $TOKEN\"",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "For AI agents, the MCP tools mirror this: rotate_generate creates a random value server-side, rotate_and_store persists a value the agent already regenerated, and list_versions shows history with [disabled] markers."
      },
      {
        "type": "prose",
        "text": "You've rotated a secret to a fresh version and listed its history. Consumers reading the latest version pick up the new value automatically, and old versions can be disabled without losing the audit trail."
      }
    ],
    "quiz": [
      {
        "question": "What's the key benefit of server-side rotation over a client PUT?",
        "options": [
          "It's faster to type",
          "The generated plaintext is never sent to or returned from the client",
          "It skips versioning",
          "It deletes the vault"
        ],
        "correctIndex": 1,
        "explanation": "Server-side rotation generates the value in the vault, so the plaintext never touches the client."
      },
      {
        "question": "What does a read of a disabled version return?",
        "options": [
          "The value normally",
          "404 Not Found",
          "410 Gone",
          "200 with an empty body"
        ],
        "correctIndex": 2,
        "explanation": "Disabled versions are kept for audit but reads return 410 Gone."
      },
      {
        "question": "Which MCP tool stores a value an agent already regenerated?",
        "options": [
          "rotate_generate",
          "rotate_and_store",
          "list_versions",
          "get_env_bundle"
        ],
        "correctIndex": 1,
        "explanation": "rotate_and_store persists a new value the agent supplies; rotate_generate makes one server-side."
      }
    ]
  },
  {
    "id": "environment-bundles",
    "title": "Environment Bundles",
    "summary": "Store many env vars as one env_bundle secret, then inject them into an app with 1claw env run or parse them with get_env_bundle.",
    "blocks": [
      {
        "type": "prose",
        "text": "Instead of storing each environment variable as its own secret, you can store a whole set as one secret of type env_bundle. Its value is KEY=VALUE lines, one per line, just like a .env file."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "The MCP tool get_env_bundle fetches an env_bundle secret, parses its KEY=VALUE lines, and returns a structured JSON object. Blank lines and lines starting with # are ignored."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Log in (or set ONECLAW_TOKEN and ONECLAW_VAULT_ID for CI).",
            "code": "1claw login",
            "lang": "bash"
          },
          {
            "text": "Write a small .env file to seed the bundle.",
            "code": "printf 'DATABASE_URL=postgres://localhost/app\\nREDIS_URL=redis://localhost:6379\\n' > app.env",
            "lang": "bash"
          },
          {
            "text": "Push those keys into the vault.",
            "code": "1claw env push app.env",
            "lang": "bash"
          },
          {
            "text": "Run your app with the vault's env injected. The process starts with DATABASE_URL and REDIS_URL set.",
            "code": "1claw env run -- node server.js",
            "lang": "bash"
          },
          {
            "text": "Confirm the injection worked with a quick echo instead of your real app.",
            "code": "1claw env run -- printenv DATABASE_URL",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "For an AI agent, point get_env_bundle at an env_bundle secret (for example config/prod-env) and it returns the variables as JSON your tool can spread into a subprocess."
      },
      {
        "type": "prose",
        "text": "You started an app with a full set of environment variables pulled straight from the vault, so config lives in 1Claw instead of a checked-in .env file."
      }
    ],
    "quiz": [
      {
        "question": "What format does an env_bundle secret's value use?",
        "options": [
          "JSON only",
          "One KEY=VALUE per line",
          "XML",
          "A single base64 blob"
        ],
        "correctIndex": 1,
        "explanation": "An env_bundle holds KEY=VALUE lines, one per line, like a .env file."
      },
      {
        "question": "Which MCP tool parses an env_bundle into a JSON object?",
        "options": [
          "get_secret",
          "get_env_bundle",
          "list_versions",
          "put_secret"
        ],
        "correctIndex": 1,
        "explanation": "get_env_bundle fetches the bundle and returns parsed KEY=VALUE lines as JSON."
      },
      {
        "question": "How do you start an app with the vault's env vars injected?",
        "options": [
          "1claw secret get",
          "1claw env run -- <cmd>",
          "1claw vault list",
          "1claw login"
        ],
        "correctIndex": 1,
        "explanation": "1claw env run -- <cmd> launches the process with the vault's secrets set as env vars."
      }
    ]
  }
]
