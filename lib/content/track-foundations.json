[
  {
    "id": "what-is-1claw",
    "title": "What is 1Claw",
    "summary": "1Claw is an HSM-backed, zero-trust vault that lets humans store secrets and lets AI agents fetch only what they're allowed to at runtime.",
    "blocks": [
      {
        "type": "prose",
        "text": "1Claw solves a specific problem: your API keys, tokens, and private keys end up pasted into .env files, prompts, and CI configs where anything can read them. 1Claw stores them in HSM-backed vaults instead, and hands them out only to principals a human explicitly allowed."
      },
      {
        "type": "points",
        "items": [
          "HSM-backed: each vault's key encryption key lives in a hardware security module",
          "Zero-trust: an agent gets zero secrets until a human writes a policy",
          "Two principals: humans own secrets, agents fetch them at runtime",
          "Same API for both at https://api.1claw.co"
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "Humans authenticate with email/password, Google, passkey, or a personal API key (1ck_). Agents authenticate with an agent API key (ocv_) that they trade for a short-lived JWT."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Sign up for a free account at the dashboard. The free tier gives you 1,000 requests/month, 3 vaults, and 2 agents.",
            "code": "https://1claw.co",
            "lang": "text"
          },
          {
            "text": "Get a personal API key from Settings → API Keys. It starts with 1ck_. Export it so the next commands can use it. Replace 1ck_your_key_here with the key you copied from the dashboard at Settings → API Keys.",
            "code": "export ONECLAW_API_KEY=\"1ck_your_key_here\"",
            "lang": "bash"
          },
          {
            "text": "Verify the key works by exchanging it for an access token against the real API. You'll get back an access_token you can use as a Bearer token.",
            "code": "curl -s -X POST https://api.1claw.co/v1/auth/api-key-token \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"api_key\\\":\\\"$ONECLAW_API_KEY\\\"}\"",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "prose",
        "text": "You now have a verified 1Claw account and a working API key. That's the credential every other lesson builds on."
      }
    ],
    "quiz": [
      {
        "question": "What backs the encryption key for each 1Claw vault?",
        "options": [
          "The agent's API key",
          "A hardware security module (HSM)",
          "A browser cookie",
          "A password in a config file"
        ],
        "correctIndex": 1,
        "explanation": "The KEK for each vault is generated inside a hardware security module and never leaves it. That is what turns a database breach into an inconvenience: the ciphertext is there and the key to it is not."
      },
      {
        "question": "By default, how much access does a new agent have?",
        "options": [
          "Read access to one vault",
          "Zero access until a human writes a policy",
          "Access to all secrets tagged public",
          "Full access to every vault"
        ],
        "correctIndex": 1,
        "explanation": "1Claw is zero-trust: agents get nothing until a human grants specific paths via a policy."
      },
      {
        "question": "Which prefix identifies a human's personal API key?",
        "options": [
          "1ck_",
          "ocv_",
          "jwt_",
          "sk_"
        ],
        "correctIndex": 0,
        "explanation": "Prefixes are not cosmetic. 1ck_ is a human personal key and ocv_ is an agent key, and the two authenticate through different endpoints with different lifetimes, so the prefix tells you which model applies."
      }
    ],
    "concepts": [
      {
        "trackId": "security-foundations",
        "lessonId": "what-is-a-secret",
        "note": "the four ways credentials leak"
      },
      {
        "trackId": "agent-threat-model",
        "lessonId": "defense-architecture",
        "note": "the control plane pattern this implements"
      }
    ]
  },
  {
    "id": "core-concepts",
    "title": "Vaults, Secrets, and Envelope Encryption",
    "summary": "Secrets live at slash-separated paths inside vaults, get a new version on every write, and are protected by a per-secret data key wrapped by the vault's HSM key.",
    "blocks": [
      {
        "type": "prose",
        "text": "A vault is a container for secrets. A secret is a value stored at a path like api-keys/openai. Every time you write to a path, 1Claw keeps the old value and bumps the version, so you always have history."
      },
      {
        "type": "points",
        "items": [
          "Vault: isolated container, has its own HSM key encryption key (KEK)",
          "Secret: value at a slash-separated path (api-keys/stripe, passwords/db)",
          "Version: every write creates a new version, newest wins on read",
          "Types: api_key, password, private_key, certificate, ssh_key, note, env_bundle"
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "Envelope encryption: each secret is encrypted with its own random data encryption key (DEK). That DEK is then wrapped by the vault's KEK, which lives in the HSM. The plaintext DEK never sits at rest."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Reuse the key you exported in Lesson 1. If it's still in your shell, skip this step.",
            "code": "export ONECLAW_API_KEY=\"1ck_your_key_here\"",
            "lang": "bash"
          },
          {
            "text": "Get an access token and save it as TOKEN.",
            "code": "export TOKEN=$(curl -s -X POST https://api.1claw.co/v1/auth/api-key-token \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"api_key\\\":\\\"$ONECLAW_API_KEY\\\"}\" | jq -r .access_token)",
            "lang": "bash"
          },
          {
            "text": "Create a vault and auto-extract the vault ID. Requires jq (brew install jq or apt install jq).",
            "code": "export VAULT_ID=$(curl -s -X POST https://api.1claw.co/v1/vaults \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"My Vault\",\"description\":\"Secrets for my app\"}' | jq -r .id)\necho \"VAULT_ID=$VAULT_ID\"",
            "lang": "bash"
          },
          {
            "text": "Store a secret at a path inside that vault.",
            "code": "curl -s -X PUT \"https://api.1claw.co/v1/vaults/$VAULT_ID/secrets/api-keys/openai\" \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"type\":\"api_key\",\"value\":\"sk-proj-demo\"}'",
            "lang": "bash"
          },
          {
            "text": "Read it back. The response includes the decrypted value plus metadata like version.",
            "code": "curl -s \"https://api.1claw.co/v1/vaults/$VAULT_ID/secrets/api-keys/openai\" \\\n  -H \"Authorization: Bearer $TOKEN\"",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "prose",
        "text": "You've now created a vault, written a secret to a path, and read the decrypted value back. That round trip is the whole 1Claw data model in action."
      }
    ],
    "quiz": [
      {
        "question": "How are secrets addressed inside a vault?",
        "options": [
          "By slash-separated paths like api-keys/stripe",
          "By numeric index",
          "By UUID only",
          "By tag name"
        ],
        "correctIndex": 0,
        "explanation": "Paths look like a filesystem because policies are written as globs over them. A path scheme chosen for readability, like api-keys/stripe, is also the thing that makes api-keys/** a meaningful grant later."
      },
      {
        "question": "In envelope encryption, what wraps the per-secret data encryption key (DEK)?",
        "options": [
          "The user's password",
          "The vault's HSM-backed key encryption key (KEK)",
          "The agent's JWT",
          "Another DEK"
        ],
        "correctIndex": 1,
        "explanation": "The KEK wraps the DEK, and only the wrapped form is stored. That indirection is what makes rotation cheap and cryptographic erasure possible: you re-encrypt or destroy a small key rather than the data."
      },
      {
        "question": "What happens when you PUT a new value to an existing secret path?",
        "options": [
          "It creates a second vault",
          "It overwrites and deletes the old value",
          "It errors because the path exists",
          "It creates a new version and keeps the old one in history"
        ],
        "correctIndex": 3,
        "explanation": "Writes are additive rather than destructive, which is what makes rotation safe. The old version stays readable while consumers migrate, and you disable it once the audit log shows nothing is still using it."
      }
    ],
    "concepts": [
      {
        "trackId": "security-foundations",
        "lessonId": "envelope-encryption",
        "note": "why vaults wrap a DEK with a KEK"
      }
    ]
  },
  {
    "id": "humans-vs-agents",
    "title": "Humans vs Agents",
    "summary": "1Claw has two principal types: humans who own secrets with 1ck_ keys, and agents who trade an ocv_ key for a short-lived JWT and see only what a policy allows.",
    "blocks": [
      {
        "type": "prose",
        "text": "There are two kinds of principal. Humans create vaults, store secrets, and write policies. Agents are AI assistants or automations that fetch secrets at runtime. Same base URL, same endpoints; only the way you authenticate and what you're allowed to see differ."
      },
      {
        "type": "points",
        "items": [
          "Human key prefix: 1ck_ (personal API key). It works directly as a Bearer token, or you can exchange it at POST /v1/auth/api-key-token.",
          "Agent key prefix: ocv_ (agent API key). Exchange it at POST /v1/auth/agent-token, sending BOTH agent_id and api_key.",
          "Do not mix them. A 1ck_ key on the agent-token endpoint, a missing agent_id, or an empty variable all return 401 Invalid credentials.",
          "Agent JWTs are short-lived (about 1 hour). Refresh by exchanging the key again.",
          "An agent with a key but no policy sees zero secrets."
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Match the key to the endpoint. A 1ck_ human key uses api-key-token (or works as a Bearer token directly). An ocv_ agent key uses agent-token and needs both agent_id and api_key. The wrong key, the wrong endpoint, or an empty shell variable all return the same 401 Invalid credentials, so check those first."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Human path. Your 1ck_ key is already a Bearer token, so you can call the API with no exchange step.",
            "code": "export ONECLAW_API_KEY=\"1ck_your_user_key\"\ncurl -s https://api.1claw.co/v1/vaults -H \"Authorization: Bearer $ONECLAW_API_KEY\"",
            "lang": "bash"
          },
          {
            "text": "Prefer a short-lived JWT for the human key? Exchange it at the api-key-token endpoint. This is a different endpoint from the agent one.",
            "code": "curl -s -X POST https://api.1claw.co/v1/auth/api-key-token \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"api_key\\\":\\\"$ONECLAW_API_KEY\\\"}\"",
            "lang": "bash"
          },
          {
            "text": "Agent path. First, create an agent using your human 1ck_ key. Use the VAULT_ID from Lesson 2, or copy a vault id from the Step 1 response. Export it if not already set. The response contains the agent_id and a plaintext api_key (starts with ocv_). Save both: the ocv_ key is shown only once.",
            "code": "# Make sure VAULT_ID is set (from Lesson 2 or Step 1 above)\nexport VAULT_ID=\"your-vault-uuid-here\"\n\nAGENT_RESPONSE=$(curl -s -X POST https://api.1claw.co/v1/agents \\\n  -H \"Authorization: Bearer $ONECLAW_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"name\\\":\\\"my-first-agent\\\",\\\"token_ttl_seconds\\\":3600,\\\"vault_ids\\\":[\\\"$VAULT_ID\\\"]}\")\necho \"$AGENT_RESPONSE\"",
            "lang": "bash"
          },
          {
            "text": "Extract agent_id and api_key from the response above. The ocv_ key is shown only at creation time: save it somewhere safe.",
            "code": "export ONECLAW_AGENT_ID=$(echo \"$AGENT_RESPONSE\" | jq -r .agent.id)\nexport ONECLAW_AGENT_KEY=$(echo \"$AGENT_RESPONSE\" | jq -r .api_key)\necho \"AGENT_ID=$ONECLAW_AGENT_ID  KEY=$ONECLAW_AGENT_KEY\"",
            "lang": "bash"
          },
          {
            "text": "Exchange the agent credentials for a short-lived JWT. Send agent_id AND api_key. Both are required. Capture the token for the next step.",
            "code": "export AGENT_TOKEN=$(curl -s -X POST https://api.1claw.co/v1/auth/agent-token \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"agent_id\\\":\\\"$ONECLAW_AGENT_ID\\\",\\\"api_key\\\":\\\"$ONECLAW_AGENT_KEY\\\"}\" | jq -r .access_token)\necho \"AGENT_TOKEN=$AGENT_TOKEN\"",
            "lang": "bash"
          },
          {
            "text": "Verify the token works by calling the agents/me endpoint. You should see the agent's metadata.",
            "code": "curl -s https://api.1claw.co/v1/agents/me \\\n  -H \"Authorization: Bearer $AGENT_TOKEN\"",
            "lang": "bash"
          },
          {
            "text": "Both paths give you a Bearer credential. Use it on the same secret endpoints. The agent only sees what a policy has granted.",
            "code": "{\n  \"access_token\": \"eyJhbGciOiJFZERTQSIs...\",\n  \"token_type\": \"Bearer\",\n  \"expires_in\": 3600\n}",
            "lang": "json"
          }
        ]
      },
      {
        "type": "prose",
        "text": "You've authenticated both ways: a human 1ck_ key used as a Bearer token, and an agent ocv_ key exchanged (with its agent_id) for a JWT. Same API, different door."
      }
    ],
    "quiz": [
      {
        "question": "Which prefix marks an agent API key?",
        "options": [
          "agt_",
          "ocv_",
          "1ck_",
          "eyJ"
        ],
        "correctIndex": 1,
        "explanation": "ocv_ marks an agent and 1ck_ marks a human. The distinction matters because an agent exchanges its key for a short-lived JWT, while a human key works directly as a bearer token."
      },
      {
        "question": "How does an agent turn its ocv_ key into a usable API credential?",
        "options": [
          "POSTs just the api_key to /v1/auth/api-key-token",
          "POSTs agent_id and api_key to /v1/auth/agent-token for a short-lived JWT",
          "Uses it directly as a permanent password",
          "Emails it to the human each time"
        ],
        "correctIndex": 1,
        "explanation": "The exchange is the point: the long-lived ocv_ key stays wherever you put it and the thing used on the wire is a short-lived JWT. Note the endpoints differ by principal, so api-key-token is the human path."
      },
      {
        "question": "What happens to an agent's active tokens when a human changes its policy?",
        "options": [
          "They last 30 days longer",
          "They're automatically revoked and must be re-exchanged",
          "They gain admin access",
          "Nothing, they keep working"
        ],
        "correctIndex": 1,
        "explanation": "Changing a policy revokes the agent's active tokens, so a narrowed scope takes effect immediately rather than whenever the current token happens to expire. Without it, revocation would be delayed by the token's lifetime."
      }
    ],
    "concepts": [
      {
        "trackId": "security-foundations",
        "lessonId": "authn-vs-authz",
        "note": "identity versus permission"
      },
      {
        "trackId": "agent-threat-model",
        "lessonId": "why-agents-break-assumptions",
        "note": "why an agent is not just another service account"
      }
    ]
  },
  {
    "id": "install-the-cli",
    "title": "Install the CLI",
    "summary": "Install @1claw/cli, log in with the browser device flow, and confirm access with whoami and vault list.",
    "blocks": [
      {
        "type": "prose",
        "text": "The CLI is the fastest way to work with 1Claw from a terminal. Install it, log in, and you'll have a working authenticated session in under a minute."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "Prerequisite: Node 20+ if you install via npm. Homebrew works on macOS and Linux and needs nothing else."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "What `1claw setup` does under the hood: it calls POST /v1/onboarding/provision, which creates the welcome bundle (a `default` vault, `examples/hello`, an MCP agent, and a `**` read/write policy) and returns a one-time `ocv_` agent key plus a ready-to-paste `mcp_stdio_config`. Track progress any time with GET /v1/org/onboarding/status. The manual steps below are the same thing, done by hand."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Fastest path: one command. `1claw setup` provisions a `default` vault, an `examples/hello` secret, an MCP agent, and a read/write policy, then writes MCP config for Cursor, Claude Desktop, and VS Code. Run it and skip to the last step.",
            "code": "npx @1claw/cli setup",
            "lang": "bash"
          },
          {
            "text": "Install the CLI with npm (needs Node 20+) or Homebrew.",
            "code": "npm install -g @1claw/cli\n# or\nbrew install 1clawAI/tap/oneclaw",
            "lang": "bash"
          },
          {
            "text": "Log in. This opens your browser to 1claw.co, you approve, and the CLI polls until you confirm. No password typed in the terminal.",
            "code": "1claw login",
            "lang": "bash"
          },
          {
            "text": "Confirm who you're logged in as.",
            "code": "1claw whoami",
            "lang": "bash"
          },
          {
            "text": "List your vaults to prove the session reaches the API.",
            "code": "1claw vault list",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "If you already exported ONECLAW_API_KEY in Lesson 1, you can skip browser login and use that key instead (see the CI tip below)."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "For CI or any non-interactive shell, skip login and set env vars instead: export ONECLAW_TOKEN=\"your-jwt\" or export ONECLAW_API_KEY=\"1ck_...\" (add ONECLAW_VAULT_ID for vault-scoped commands)."
      },
      {
        "type": "prose",
        "text": "The CLI is installed and authenticated. 1claw whoami and 1claw vault list both work, so you're ready to create vaults and secrets from the terminal."
      }
    ],
    "quiz": [
      {
        "question": "What Node version does the npm install of the CLI require?",
        "options": [
          "Node 14+",
          "Any version",
          "Node 20+",
          "Node 16+"
        ],
        "correctIndex": 2,
        "explanation": "Node 20 or newer for the npm install. Homebrew bundles its own runtime, so it is the simpler route if your system Node is older or you would rather not manage it."
      },
      {
        "question": "What does 1claw login use so you never type a password in the terminal?",
        "options": [
          "An SSH key",
          "A one-time SMS",
          "A QR code scanner",
          "A browser device flow"
        ],
        "correctIndex": 3,
        "explanation": "The browser device flow means the credential is never typed into a terminal, never lands in shell history, and never appears in a screen recording. The CLI polls until you approve."
      },
      {
        "question": "Which env var lets the CLI run non-interactively with a personal API key in CI?",
        "options": [
          "ONECLAW_PASSWORD",
          "ONECLAW_API_KEY",
          "ONECLAW_LOGIN",
          "CLAW_SESSION"
        ],
        "correctIndex": 1,
        "explanation": "ONECLAW_API_KEY (or ONECLAW_TOKEN) skips the interactive flow, which is what CI needs. The other options are distractors: none of them exist."
      }
    ]
  },
  {
    "id": "start-local-no-account",
    "title": "Start local: a vault with no account",
    "summary": "Encrypt secrets on your own machine with 1claw local, before you sign up for anything.",
    "blocks": [
      {
        "type": "prose",
        "text": "You do not need an account, a network, or a credit card to get value from 1Claw. The CLI ships a local encrypted vault that works entirely offline, and it is the right place for a developer to start: it solves the .env-file problem on your own laptop first, and everything you learn transfers to the hosted vault later."
      },
      {
        "type": "points",
        "items": [
          "The vault is a single encrypted file at ~/.config/1claw/local-vault.enc.",
          "It is AES-256-GCM, with the key derived from your passphrase by scrypt.",
          "The file is written with 0600 permissions, so only your user can read it.",
          "Nothing leaves your machine unless you explicitly run a sync."
        ]
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Create the vault. You will be prompted for a passphrase twice; it must be at least 8 characters.",
            "code": "1claw local init",
            "lang": "bash"
          },
          {
            "text": "Add a secret. You are prompted for the value rather than passing it as an argument, which keeps it out of your shell history.",
            "code": "1claw local add stripe/test-key",
            "lang": "bash"
          },
          {
            "text": "List what is stored. Names only, never values.",
            "code": "1claw local list",
            "lang": "bash"
          },
          {
            "text": "Read one back.",
            "code": "1claw local get stripe/test-key\n\n# or masked, when someone is looking over your shoulder\n1claw local get stripe/test-key --masked",
            "lang": "bash"
          },
          {
            "text": "Check the vault's state at any time.",
            "code": "1claw local status",
            "lang": "bash"
          },
          {
            "text": "Remove a secret you no longer need.",
            "code": "1claw local rm stripe/test-key",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Unlocking is deliberately slow. scrypt is configured with a large work factor, so each command takes a moment while it derives the key. That cost is what makes a stolen vault file expensive to brute force."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "The passphrase cannot be recovered. There is no reset, no support path, and no escrow: if you forget it, the only option is `1claw local destroy`, which deletes the vault so you can start over. Put the passphrase in your password manager before you add anything you care about."
      },
      {
        "type": "prose",
        "text": "This is the whole local story, and for a solo developer it may be all you ever need. The next two lessons cover the two things it cannot do: share a secret with a teammate, and give an agent scoped access. Both are what the hosted vault is for."
      }
    ],
    "quiz": [
      {
        "question": "Where does the local vault store its data?",
        "options": [
          "In a hidden folder in the current project",
          "On 1Claw's servers, encrypted client-side",
          "In a single encrypted file at ~/.config/1claw/local-vault.enc",
          "In your system keychain"
        ],
        "correctIndex": 2,
        "explanation": "It is one AES-256-GCM file written with 0600 permissions. Nothing is transmitted anywhere until you explicitly run a sync."
      },
      {
        "question": "You forget your local vault passphrase. What are your options?",
        "options": [
          "Recover it through the dashboard",
          "Support can decrypt it with the master key",
          "Reset it with your account email",
          "None; the only path is `1claw local destroy` and starting over"
        ],
        "correctIndex": 3,
        "explanation": "The passphrase is the only key material and it is never stored. destroy exists precisely because deletion is the only available recovery, which is why the passphrase belongs in a password manager."
      },
      {
        "question": "Why does each local command take a noticeable moment?",
        "options": [
          "It re-encrypts the whole vault on every read",
          "It verifies the file against a remote checksum",
          "scrypt derives the key with a large work factor, which is what makes brute forcing the file expensive",
          "It checks for updates over the network"
        ],
        "correctIndex": 2,
        "explanation": "The delay is the security property. A fast key derivation would make an offline attack against a stolen vault file cheap."
      }
    ]
  },
  {
    "id": "lab-is-it-really-encrypted",
    "title": "Lab: check that the vault file is opaque",
    "summary": "Take the claim that the local vault is encrypted at rest and verify it yourself with strings, xxd and a wrong passphrase.",
    "blocks": [
      {
        "type": "prose",
        "text": "Two lessons ago you created a local vault and were told the file is encrypted with a passphrase-derived key. That is a claim about a file on your disk, and claims about files on your disk can be checked rather than believed."
      },
      {
        "type": "callout",
        "variant": "info",
        "text": "This uses openssl on a file you create, so it works whether or not you set up a local vault. The construction is the same shape: a passphrase, a key derivation, and an authenticated cipher."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Write a plaintext env file and encrypt it with a passphrase.",
            "code": "mkdir -p /tmp/vlab && cd /tmp/vlab\nprintf 'STRIPE_KEY=sk-live-9f2a\\nDB_PASSWORD=prod-pw-42\\n' > plain.env\nopenssl enc -aes-256-cbc -pbkdf2 -iter 200000 \\\n  -pass pass:correct-horse -in plain.env -out vault.enc",
            "lang": "bash"
          },
          {
            "text": "Look at the plaintext file the way anyone with read access would.",
            "code": "strings plain.env",
            "lang": "bash"
          },
          {
            "text": "Now do the same to the encrypted one. Count the matches rather than trusting your eyes.",
            "code": "strings vault.enc | grep -c 'sk-live\\|prod-pw'\n# -> 0",
            "lang": "bash"
          },
          {
            "text": "Look at the actual bytes. The Salted__ header is the only thing that is not ciphertext.",
            "code": "xxd vault.enc | head -2",
            "lang": "bash"
          },
          {
            "text": "Try a wrong passphrase. It fails rather than returning garbage, because the padding check catches it.",
            "code": "openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 \\\n  -pass pass:wrong-guess -in vault.enc\n# -> bad decrypt",
            "lang": "bash"
          },
          {
            "text": "And the right one.",
            "code": "openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 \\\n  -pass pass:correct-horse -in vault.enc",
            "lang": "bash"
          },
          {
            "text": "Clean up.",
            "code": "cd /tmp && rm -rf vlab",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "prose",
        "text": "Three things worth taking from a check that took two minutes."
      },
      {
        "type": "points",
        "items": [
          "The salt is stored in the clear at the start of the file, and it is not secret. It exists so that the same passphrase produces a different key for every file, which is what stops one precomputed table from opening all of them.",
          "The iteration count is the cost you pay on every unlock and the cost an attacker pays on every guess. 200000 here, and a deliberately large scrypt work factor in the real local vault, are the same idea.",
          "A wrong passphrase produced an error rather than plausible-looking wrong output. That is the integrity check doing its job, and it is why authenticated modes are preferred."
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "None of this protects a file someone can read while it is unlocked, or a passphrase in your shell history. Encryption at rest answers exactly one question: what an attacker gets from the file alone."
      }
    ],
    "quiz": [
      {
        "question": "The salt sits in the clear at the start of the file. Is that a weakness?",
        "options": [
          "Yes, it lets an attacker skip the key derivation",
          "Yes, it should be stored separately",
          "No, because it is hashed",
          "No: it is not secret, and it makes the same passphrase derive a different key per file"
        ],
        "correctIndex": 3,
        "explanation": "A salt defeats precomputation. Its job is uniqueness rather than secrecy, which is why every mainstream format stores it alongside the ciphertext."
      },
      {
        "question": "What is the iteration count actually buying?",
        "options": [
          "Stronger ciphertext",
          "A longer key",
          "Cost per guess: you pay it once per unlock, an attacker pays it on every attempt",
          "Protection against a lost passphrase"
        ],
        "correctIndex": 2,
        "explanation": "It makes the key derivation deliberately slow, which is negligible for one legitimate unlock and expensive across a brute-force search."
      },
      {
        "question": "A wrong passphrase produced 'bad decrypt' rather than garbage. Why does that matter?",
        "options": [
          "It confirms the file is not corrupt",
          "The integrity check detects the wrong key instead of returning plausible-looking wrong data",
          "It means the passphrase was close",
          "It proves the salt was correct"
        ],
        "correctIndex": 1,
        "explanation": "Silent garbage on a wrong key is how corrupted data gets written back as though it were valid. Detecting the failure is the property authenticated modes provide."
      }
    ],
    "kind": "lab"
  },
  {
    "id": "bring-your-dotenv",
    "title": "Bring your existing .env",
    "summary": "Import the .env files you already have, then export them back on demand so your tooling never changes.",
    "blocks": [
      {
        "type": "prose",
        "text": "Nobody starts from an empty vault. You start with a handful of .env files scattered across projects, which is the exact problem worth solving, so the local vault imports them directly."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Look before you leap. A dry run shows what would be imported without writing anything.",
            "code": "1claw local import .env --dry-run",
            "lang": "bash"
          },
          {
            "text": "Import for real. Existing entries are skipped rather than overwritten unless you say otherwise.",
            "code": "1claw local import .env",
            "lang": "bash"
          },
          {
            "text": "If you do want to overwrite what is already there, be explicit about it.",
            "code": "1claw local import .env --force",
            "lang": "bash"
          },
          {
            "text": "Export back to .env format when a tool needs a file. Without -o it writes to stdout, so you can pipe it.",
            "code": "1claw local export\n\n# or straight to a file your tooling expects\n1claw local export -o .env",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Once a secret is in the vault, delete the .env from the project and add it to .gitignore. Importing without removing the original leaves you with two copies and no improvement."
      },
      {
        "type": "prose",
        "text": "The import and export pair is deliberately boring, and that is the point: it means adopting the vault does not require changing how any of your tools work. You keep the .env-shaped interface at the edges and stop keeping plaintext files around between runs."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "`1claw env run -- <command>` goes one step further: it resolves the values and injects them into a child process's environment, so nothing is ever written to disk at all."
      }
    ],
    "quiz": [
      {
        "question": "What does `1claw local import .env --dry-run` do?",
        "options": [
          "Imports only the keys that do not already exist",
          "Shows what would be imported without writing anything",
          "Validates the .env syntax and exits",
          "Imports into a temporary vault for testing"
        ],
        "correctIndex": 1,
        "explanation": "It is a preview. Running it first is worth the habit on any file you have not read recently."
      },
      {
        "question": "By default, what happens to a key that already exists in the vault?",
        "options": [
          "Both values are kept as separate versions",
          "The import aborts with an error",
          "It is skipped, unless you pass --force",
          "It is overwritten with the .env value"
        ],
        "correctIndex": 2,
        "explanation": "Skipping is the safe default, since the vault copy is more likely to be current than a file that has been sitting in a project directory."
      },
      {
        "question": "What should you do with the .env file after importing it?",
        "options": [
          "Move it to a shared drive",
          "Keep it as a backup",
          "Encrypt it with the same passphrase",
          "Delete it and add it to .gitignore, or the import achieved nothing"
        ],
        "correctIndex": 3,
        "explanation": "Two copies is the sprawl problem the vault exists to remove. The value only improves once the plaintext original is gone."
      }
    ]
  },
  {
    "id": "lab-leak-into-git",
    "title": "Lab: delete a secret and watch it survive",
    "summary": "Commit a .env, remove it, add a .gitignore, then read the secret straight back out of the repository.",
    "blocks": [
      {
        "type": "prose",
        "text": "The previous lesson told you to delete the .env after importing it. This lab is why that instruction has a second half, and why the second half is the one people skip."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Make a throwaway repository and commit a .env, exactly as it happens by accident.",
            "code": "mkdir -p /tmp/gitlab && cd /tmp/gitlab\ngit init -q . && git config user.email you@example.com && git config user.name You\nprintf 'STRIPE_KEY=sk-live-9f2a-LEAKED\\n' > .env\ngit add .env && git commit -qm \"add config\"",
            "lang": "bash"
          },
          {
            "text": "Notice the mistake and do the obvious cleanup: remove the file, add a .gitignore, commit.",
            "code": "git rm -q .env\nprintf '.env\\n' > .gitignore\ngit add .gitignore && git commit -qm \"remove secret, add gitignore\"\nls -a",
            "lang": "bash"
          },
          {
            "text": "The working tree is clean. The secret is not gone.",
            "code": "git log -p --all | grep -o 'sk-live[^ ]*'",
            "lang": "bash"
          },
          {
            "text": "Read it directly out of the earlier commit, which is what anyone who clones the repository can do.",
            "code": "git show HEAD~1:.env\n# -> STRIPE_KEY=sk-live-9f2a-LEAKED",
            "lang": "bash"
          },
          {
            "text": "Clean up.",
            "code": "cd /tmp && rm -rf gitlab",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "prose",
        "text": "Deleting a file removes it from the current commit and from nothing else. Git is designed to preserve history, and it did its job perfectly."
      },
      {
        "type": "points",
        "items": [
          "Anyone who has ever cloned or forked the repository already has the secret, and nothing you do to your copy reaches theirs.",
          "Rewriting history with filter-repo or BFG changes the object graph and does not un-distribute what was pushed, and it does not touch a fork.",
          "On a hosted platform the old objects may remain reachable by commit SHA long after the branch is rewritten."
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "The only response that actually works is rotation. Treat a committed secret as burned from the moment it is pushed, rotate it, and clean the history afterwards as tidying rather than as the fix."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Add .env to .gitignore in your project template rather than after the first mistake. A gitignore added in commit two protects nothing that happened in commit one."
      },
      {
        "type": "prose",
        "text": "This is the fourth way a secret escapes that this course has shown you and none of them involved an attacker doing anything clever: a build log, a process table, a plaintext file, and now version control that is faithfully doing what it was built to do."
      }
    ],
    "quiz": [
      {
        "question": "You deleted the .env and committed. Why is the secret still exposed?",
        "options": [
          "The .gitignore only applies to new files",
          "The delete was not pushed",
          "Git preserves history, so earlier commits still contain the file",
          "The file is cached in the index"
        ],
        "correctIndex": 2,
        "explanation": "Deleting removes the file from the current commit and from nothing else. Anyone who can read the repository can read any earlier commit."
      },
      {
        "question": "What is the only response that reliably works?",
        "options": [
          "Force-pushing over the branch",
          "Making the repository private",
          "Rewriting history with filter-repo",
          "Rotating the credential, then cleaning history as tidying"
        ],
        "correctIndex": 3,
        "explanation": "History rewriting does not reach clones, forks or objects still addressable by SHA on the host. Once pushed, treat the secret as burned."
      },
      {
        "question": "When should .env go into .gitignore?",
        "options": [
          "Immediately after the first accidental commit",
          "In the project template, before the first commit",
          "Once the repository is made public",
          "Only if the file contains production values"
        ],
        "correctIndex": 1,
        "explanation": "A gitignore added in commit two protects nothing that happened in commit one, which is exactly when the mistake is made."
      }
    ],
    "kind": "lab"
  },
  {
    "id": "first-vault-and-secret",
    "title": "Your First Vault and Secret",
    "summary": "Use the CLI to create a vault, store a secret, and read it back, with the dashboard as an alternative.",
    "blocks": [
      {
        "type": "prose",
        "text": "Now that the CLI is authenticated, storing and reading a secret is three commands. This is the everyday human workflow, and it is worth noticing what the vault gives you that the local one from two lessons ago could not: the secret now exists somewhere other than your laptop, with an access decision and an audit record attached to every read."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Confirm you're logged in (from the install lesson).",
            "code": "1claw whoami",
            "lang": "bash"
          },
          {
            "text": "Create a vault. The command prints the new vault's id.",
            "code": "1claw vault create \"My Vault\"",
            "lang": "bash"
          },
          {
            "text": "Export the vault ID from the create output so subsequent commands target this vault. If you already created a vault in Lesson 2, you can reuse it: run 1claw vault list and export that vault's UUID instead of creating a new one.",
            "code": "export ONECLAW_VAULT_ID=\"paste-vault-id-from-above\"",
            "lang": "bash"
          },
          {
            "text": "Store a secret at a path. Give it a type so 1Claw knows what it is. The CLI uses ONECLAW_VAULT_ID to pick the vault.",
            "code": "1claw secret set api-keys/openai --value \"sk-proj-demo\" --type api_key --vault \"$ONECLAW_VAULT_ID\"",
            "lang": "bash"
          },
          {
            "text": "Read the value back.",
            "code": "1claw secret get api-keys/openai --vault \"$ONECLAW_VAULT_ID\"",
            "lang": "bash"
          },
          {
            "text": "List the secrets in the vault (metadata only, never values).",
            "code": "1claw secret list --vault \"$ONECLAW_VAULT_ID\"",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "prose",
        "text": "Give some thought to the path. Policies are globs over paths, so the scheme you pick now decides what you can grant later. Grouping by provider, as in api-keys/openai and api-keys/stripe, means api-keys/** is a meaningful grant. Grouping by application instead means the same glob hands an agent everything that application uses. Neither is wrong; they simply make different grants easy."
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Avoid putting the environment at the end of a path, as in stripe/key/prod. Anything scoped to stripe/** then reaches production, and the only way to carve it out is a deny rule someone has to remember to write. Put the distinguishing part where a glob can stop at it."
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Prefer clicking? In the dashboard at 1claw.co, go to Vaults then Create vault, then Secrets then Add secret at a path like api-keys/openai. It hits the same endpoints as the CLI."
      },
      {
        "type": "prose",
        "text": "You have a vault with a stored secret you can read on demand, and every one of those reads is now attributable. The next lesson syncs your local vault into this one; after that the course turns to the thing only a hosted vault can do, which is give something other than you a scoped, revocable way in."
      }
    ],
    "quiz": [
      {
        "question": "Which CLI command stores a secret at a path?",
        "options": [
          "1claw vault create",
          "1claw secret set",
          "1claw env pull",
          "1claw whoami"
        ],
        "correctIndex": 1,
        "explanation": "The verb is set, not put. It takes the path as a positional argument and the value either positionally, via --value, or from stdin, which keeps it out of your shell history."
      },
      {
        "question": "What does 1claw secret list return?",
        "options": [
          "Only the vault name",
          "Metadata only, no values",
          "Decrypted values for every secret",
          "The vault's KEK"
        ],
        "correctIndex": 1,
        "explanation": "Listing returns paths, types and versions and never values. That separation is what lets you grant an agent the ability to discover what exists without the ability to read it."
      },
      {
        "question": "The dashboard is best described as what?",
        "options": [
          "A replacement for HSMs",
          "A UI on top of the same API endpoints the CLI uses",
          "A separate database",
          "An offline-only tool"
        ],
        "correctIndex": 1,
        "explanation": "The dashboard calls the same endpoints as the CLI and the SDK. Nothing is available only through the UI, which is why anything you do by hand can later be scripted."
      }
    ],
    "concepts": [
      {
        "trackId": "security-foundations",
        "lessonId": "envelope-encryption",
        "note": "what happens to the value at rest"
      }
    ]
  },
  {
    "id": "from-local-to-cloud",
    "title": "From local to cloud",
    "summary": "Push your local vault to a hosted one when you need a second person, a second machine, or an agent.",
    "blocks": [
      {
        "type": "prose",
        "text": "The local vault has exactly three limits, and you hit them in a predictable order: you get a second machine, then a teammate, then an agent. All three need a vault that exists somewhere other than your laptop."
      },
      {
        "type": "points",
        "items": [
          "A second machine needs the secret to exist off your disk.",
          "A teammate needs sharing, access control and an audit trail.",
          "An agent needs a scoped identity, which is the whole rest of this course."
        ]
      },
      {
        "type": "callout",
        "variant": "warn",
        "text": "Requires: a 1Claw account and a cloud vault. `npx @1claw/cli setup` creates one along with everything else in a single command."
      },
      {
        "type": "steps",
        "steps": [
          {
            "text": "Log in. The browser flow means no password is typed into your terminal.",
            "code": "1claw login",
            "lang": "bash"
          },
          {
            "text": "Preview the move. Push is the default direction: local to cloud.",
            "code": "1claw local sync --vault $VAULT_ID",
            "lang": "bash"
          },
          {
            "text": "Pull instead, to seed a fresh laptop from a vault that already exists.",
            "code": "1claw local sync --vault $VAULT_ID --pull",
            "lang": "bash"
          },
          {
            "text": "Conflicts are skipped by default. Overwrite only when you are sure which side is authoritative.",
            "code": "1claw local sync --vault $VAULT_ID --force",
            "lang": "bash"
          },
          {
            "text": "Confirm the cloud side has what you expect.",
            "code": "1claw secret list --vault $VAULT_ID",
            "lang": "bash"
          }
        ]
      },
      {
        "type": "callout",
        "variant": "tip",
        "text": "Sync skips conflicts rather than guessing. That is the right default and it means a sync can silently do less than you assumed, so read the summary rather than trusting the exit code."
      },
      {
        "type": "prose",
        "text": "Keeping the local vault after syncing is a reasonable choice. It works offline, it survives an outage, and it costs nothing. What changes is that the cloud vault becomes the authoritative copy once more than one person or machine depends on it."
      },
      {
        "type": "prose",
        "text": "From here the course is about the things only a hosted vault can do: policies that scope access per principal, agents as first-class identities with short-lived tokens, execution without ever handing over a credential, and an audit trail that answers who read what. The local vault got you started without a signup; the rest of this course is why teams outgrow it."
      }
    ],
    "quiz": [
      {
        "question": "Which direction does `1claw local sync` move secrets by default?",
        "options": [
          "Bidirectional merge",
          "It prompts for a direction each time",
          "Local to cloud; --pull reverses it",
          "Cloud to local"
        ],
        "correctIndex": 2,
        "explanation": "Push is the default because the common first case is seeding a hosted vault from the laptop where the secrets already are."
      },
      {
        "question": "What happens by default when a secret exists on both sides with different values?",
        "options": [
          "The newer value wins",
          "The conflict is skipped unless you pass --force",
          "The sync aborts entirely",
          "Both are kept as versions"
        ],
        "correctIndex": 1,
        "explanation": "Skipping avoids silently destroying a value. It also means a sync can do less than you expected, so the summary is worth reading."
      },
      {
        "question": "What is the first thing the local vault cannot give you?",
        "options": [
          "Support for .env files",
          "A way to list secrets",
          "Encryption at rest",
          "A secret that exists anywhere other than your own disk"
        ],
        "correctIndex": 3,
        "explanation": "Local encryption is genuinely good. The limits are all about existing in more than one place: a second machine, a teammate, or an agent."
      }
    ]
  }
]
